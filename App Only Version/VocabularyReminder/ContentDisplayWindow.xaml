<Window x:Class="VR.ContentDisplayWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:md="clr-namespace:MdXaml;assembly=MdXaml"
        mc:Ignorable="d"
        Title="Content Display"
        Width="800"
        Height="600"
        MinWidth="500"
        MinHeight="400"
        Background="#1E1E1E"
        WindowStyle="SingleBorderWindow"
        ShowInTaskbar="True"
        ResizeMode="CanResize"
        WindowStartupLocation="CenterOwner">
    
    <Window.Resources>
        <Style TargetType="Button">
            <Setter Property="Background" Value="#333333"/>
            <Setter Property="Foreground" Value="#FFFFFF"/>
            <Setter Property="BorderThickness" Value="0"/>
            <Setter Property="Padding" Value="10,6"/>
            <Setter Property="Margin" Value="4,8"/>
            <Setter Property="MinWidth" Value="70"/>
            <Setter Property="FontSize" Value="12"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border Background="{TemplateBinding Background}"
                                BorderThickness="0"
                                CornerRadius="3">
                            <ContentPresenter HorizontalAlignment="Center"
                                            VerticalAlignment="Center"/>
                        </Border>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
            <Style.Triggers>
                <Trigger Property="IsMouseOver" Value="True">
                    <Setter Property="Background" Value="#444444"/>
                </Trigger>
                <Trigger Property="IsPressed" Value="True">
                    <Setter Property="Background" Value="#555555"/>
                </Trigger>
            </Style.Triggers>
        </Style>
    </Window.Resources>
    
    <DockPanel>
        <!-- Header -->
        <Border DockPanel.Dock="Top" Background="#2D2D30" BorderBrush="#404040" BorderThickness="0,0,0,1">
            <TextBlock x:Name="TitleText"
                       Text="Content"
                       FontSize="15"
                       FontWeight="SemiBold"
                       Foreground="#FFFFFF"
                       VerticalAlignment="Center"
                       Margin="20,12"
                       Height="21"/>
        </Border>
        
        <!-- Content Area -->
        <Border Background="#1E1E1E" Padding="20">
            <md:MarkdownScrollViewer x:Name="ContentDisplay"
                                     Background="Transparent"
                                     Foreground="#FFFFFF"
                                     FontFamily="Segoe UI"
                                     FontSize="14"
                                     BorderThickness="0"
                                     VerticalScrollBarVisibility="Auto"
                                     HorizontalScrollBarVisibility="Disabled">
                <md:MarkdownScrollViewer.MarkdownStyle>
                    <Style TargetType="FlowDocument">
                        <Setter Property="Background" Value="Transparent"/>
                        <Setter Property="Foreground" Value="#FFFFFF"/>
                        <Setter Property="FontFamily" Value="Segoe UI"/>
                        <Setter Property="FontSize" Value="14"/>
                        <Setter Property="LineHeight" Value="22"/>
                        <Setter Property="PagePadding" Value="0"/>
                    </Style>
                </md:MarkdownScrollViewer.MarkdownStyle>
            </md:MarkdownScrollViewer>
        </Border>
    </DockPanel>
</Window>